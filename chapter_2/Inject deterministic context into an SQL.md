Inject deterministic context into an SQL agent prompts
So far, we have used our baseline prompt template, combining the system and user prompts, and injecting the user question and the table schema as arguments. This information may be sufficient in some cases to generate the correct SQL queries. On the other hand, as we saw in the previous lesson, it may introduce ambiguity leading the LLM to estimate the wrong values. And this lesson will address this issue by introducing new argument to the prompt template. This will enable us to add an additional context or information to the system template when needed. For this demo, we'll use the 02 underscore 04B Jupyter notebook in the chapter 2 folder. We'll keep the same setup as before. Use Postgres as the database and GPT-4 as the LLM model. As before, to run this notebook with DuckDB instead of Postgres, uncomment this code and run it. Let's execute the code. Now let's modify the prompt template by adding a new argument called additionalContext, which is placed over here. As the name implies, this argument allows us to inject extra information into the prompt to reduce ambiguity, provide deterministic hints, and set the model up for success. Let's update the new prompt and also update the chain. Let's update the basic SQL agent function. We'll add this new argument, additional context, and set the default value to an empty string. Let's update the basic SQL agent function. We'll add this new argument and set the default value to an empty string. If we ask the transit question without any additional context, the model will return the same answer as before. And you can see this additional context argument set to an empty string. And it returned the wrong answer, as the model didn't have the right context about the right value for the transit passengers. Now let's see what happens when we do provide extra context. We'll start by querying the database to extract the distinct values of the activity type code column. Let's run this query and it returns the unique values. For this, we can see that the correct value for transit passengers is through slash transit. We'll then pass the following text as additional context. The activity type code unique values are deep land and planned and through slash transit. Let's run this, and as you can see, it returned the correct answer using the correct code for filtering the activity type code column. At this point, you might be thinking, Rami, are you expecting users to provide this information themselves? The short answer is no. The goal here is not to rely on users, but to programmatically inject useful context when the model needs it. Your job as a system designer is to identify scenarios where the LLM lacks sufficient information and to automate the enrichment of the prompt accordingly. In this case, I created a set of helper functions for this task. The GetCharacterDistinctValues, which extracts unique values from categorical fields, And the formatDistinctValuesForPrompt, which as the name implies, reformats them into a prompt-friendly structure. Those functions are available in the SQL AI agent module in the course folder. Let's import them and see how they are working. First, we are pulling the distinct values and you can see it returns this list. Next, we'll reformat them. And as you can see, this is the structure we are going to use for the prompt, providing the additional context and information. Last but not least, we will inject this object as an input to the additional context argument. And as you can see, it returned the correct answer. One important thing to keep in mind is that adding more context increases the number of tokens sent with each prompt. Later in this course, we'll discuss strategies for managing token usage efficiently and deciding when additional context is worth the cost. In the next lesson, we'll learn how to add memory to the SQL AI agent, enabling natural follow-up questions and multi-turn interactions.
