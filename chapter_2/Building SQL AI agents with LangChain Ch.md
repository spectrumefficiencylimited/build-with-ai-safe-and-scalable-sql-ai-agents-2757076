Building SQL AI agents with LangChain Chains
In the previous lesson, we saw how to templatize a prompt using LangChain's dedicated classes. This setup will allow us leverage the broader LangChain ecosystem and compose more advanced workflow using chains. A LangChain chain let us connect multiple steps such as prompt formatting, LLM invocation, and post-processing into a one runnable unit. it. In this lesson, we'll build a chain that sends the PROM template to an LLM API and extracts the generated SQL query. For this demo, we'll use the 02-02b Jupyter notebook in the chapter 2 folder, and I will run it using the course virtual environment. Let's start by setting up the the database access. I will use the getIbisConnection function to connect to a Postgres database and set the table name as AirTraffic. Let's execute this code chunk and this code chunk. Now that we are connected to the Postgres, we can continue to the next step. If you prefer using DuckDB instead of Postgres, you can simply uncomment the alternative code provided below and run it. Next we'll define the prompt template, using the same approach as in the previous lesson. Now let's set a table schema. This time, instead of defining it manually, we'll use a helper function called getTableAttribute to extract metadata directly from the database. This function derived information such as column names and types, allowing us to automate the process and eliminate manual steps. Let's run the function and assign the results to the schema variable. Next, we will configure the LLM client using the ChatOpenAI class. This allows us to connect to any model that is compatible with the OpenAI SDK, which includes most major LLM, such as OpenAI, Google Gemini, and Atropic. We can switch between models by updating the best URL, the model name, and the API key. In this example, we will use the OpenAI GPT-4.0 model. I will use the getEnv method to extract my API key from an environment variable. Now that we have both the PROM template and the LLM chain, we can combine them into a chain using the pipe operator. If we print the chain, you can notice that it automatically iterates the input variables defined in the prompt template, question, schema, table name. Before invoking the chain, let's define our question. How many passengers landed during 2024? We can now invoke the chain by passing the required arguments, the question, table name and schema. Let's pause for a moment and look at what's happening behind the scenes. The chance first format the prompt using the provided inputs. It then sends the prompt to the LLM through the client as a get request and retains the model's response, which in our case should be a valid SQL query. Let's print the response. As you can see, the model generated a SQL query that answers our question about passengers landing in 2024. We can now validate the query by sending it to the database using our connection object. And it returns the expected results. Before moving on to the next topic, let's wrap these steps into a simple helper function. We'll call it basic SQL agent. And we pass the following argument, chain, question, table name, schema, and the connection object. The function under hood, just repeat the same steps and print the output in a nice format. Let's run the function. And now, let's ask the same question. How many passengers landed during 2024? And as you can see, it returns the output in a clean and readable format. At this point, we have a complete end-to-end loop from a natural language question to SQL generation to database execution. Congratulations, you have just built your first SQL agent in this course.
